# Web Dev Starter Code

## Overview

A simple inventory management web app built with plain HTML, CSS, and JavaScript. 
It connects to an AWS Lambda–powered API Gateway endpoint to perform basic CRUD 
operations: loading, adding, and deleting items. The app displays inventory data 
in a clean table and includes a form to add new entries.

## Sources and Credits

A simple bulleted list below is sufficient. For example:

- Starter HTML/JS structure based on class starter template
